extends layout

block content
  h1= title

  form(method='POST')
    .form-group
      button.btn.btn-primary(type='submit') Create

  h3 Search
    form(method='POST')
      .form-group
        input#charname.form-control(type='text', placeholder='Turin Deathstalker' name='charname' )    
        button.btn.btn-primary(type='submit') Search

  h3 Browse
  .sortbutton-group 
    button.btn.btn-primary(type='submit') Sort By Name
    button.btn.btn-primary(type='submit') Sort By Class
    button.btn.btn-primary(type='submit') Sort By Level

  .button-group 
    button.btn.btn-primary(type='submit') Back
    button.btn.btn-primary(type='submit') Forward